<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_advance" model="ir.ui.view">
        <field name="name">account.move.form.advance</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <button name="action_register_payment" position="before">
                <button name="action_open_advance_settle_wizard"
                        type="object"
                        string="تسوية دفعة مقدمة"
                        class="btn-secondary"
                        invisible="move_type != 'out_invoice' or state != 'posted' or payment_state in ('paid','in_payment','reversed') or available_advance_amount &lt;= 0"/>
            </button>
        </field>
    </record>
</odoo>
